{
  "name": "diu-english-smartroutine",
  "private": true,
  "version": "1.0.0",
  "description": "DIU Department of English · SmartRoutine — frontend (React) + backend (Express/MongoDB)",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "install:all": "npm --prefix frontend install && npm --prefix backend install",
    "build": "npm --prefix frontend run build && node ./scripts/copy-web-dist.mjs",
    "start": "npm --prefix backend start",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:windows": "powershell -ExecutionPolicy Bypass -File ./start-dev.ps1"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}

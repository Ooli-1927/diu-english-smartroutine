# Backend API (leave unset in development — Vite proxies /api to VITE_API_TARGET)
# VITE_API_URL=https://your-api.example.com/api
# VITE_API_TARGET=http://127.0.0.1:4000

# Optional Supabase fallback, used only when the API above is unreachable
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
